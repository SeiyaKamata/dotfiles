cache=${XDG_CACHE_HOME}/npm

# Font Awesome Pro（トークン実体は zsh/.zshenv.local）
//npm.fontawesome.com/:_authToken=${NPM_TOKEN}

# システム導入の node はグローバル prefix が root 所有(/usr など)で `npm i -g` が
# sudo なしだと EACCES で落ちる。prefix をユーザー書き込み可能な ~/.local に逃がす（bin は既に PATH 上）。
prefix=${HOME}/.local
