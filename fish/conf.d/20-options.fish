# Fish shell options and key bindings

# Set vim-like keybindings (equivalent to bindkey -e in zsh is default fish behavior)
# Fish uses emacs-like keybindings by default

# Enable command completions (fish has this built-in)
# Fish automatically loads completions from various sources
