---
name: commit
description: 変更内容を確認してコミットプランを提示し、承認後にコミットする。
---

# コミット

## 役割
変更内容を確認してコミットプランを人間に提示し、承認後にコミットする。

## 進め方
1. 変更ファイルを確認する（`git status`, `git diff`）
2. 変更を作業の流れが伝わる単位に分割し、コミットプランを組み立てる
3. コミットプランを人間に提示し、承認を得る
4. 承認後に順番にコミットを実行する

## コミットプランの形式

コミットは「なぜその順番か」が伝わるように並べる。
例: リネーム → 内容修正 → 新規追加、のように依存関係・論理的な流れを意識する。

```
## コミットプラン

1. <type>(<scope>): <subject>
   対象ファイル: foo.md, bar.md
   内容: ...

2. <type>(<scope>): <subject>
   対象ファイル: baz.md
   内容: ...
```

承認を得てから実行に移る。

## コミットメッセージの規則
Conventional Commits形式を使用する。

```
<type>(<scope>): <subject>

<body>

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

**type一覧:**
- `feat`: 新機能
- `fix`: バグ修正
- `refactor`: リファクタリング
- `docs`: ドキュメント
- `test`: テスト
- `chore`: ビルド・設定変更

**subject:** 変更内容を一言で（命令形・現在形）

**body:** レビュアーが背景・意図・影響範囲を理解できるように記述する。以下を意識する：
- なぜこの変更が必要だったか（背景・問題）
- 何をどう変えたか（具体的な変更内容）
- 影響範囲や注意点があれば記載

**注意:**
- コミットメッセージはHEREDOCで渡す（特殊文字のエスケープを防ぐため）
- `Co-Authored-By` 行を必ず末尾に付与する

## 完了条件
コミット完了後に報告する。
